import React from "react";
import { StyledCustomButton } from "./styles";

const CustomButton = ({ children }) => {
  return <StyledCustomButton>{children}</StyledCustomButton>;
};

export default CustomButton;
