import styled from "styled-components";

export const StyledClasses = styled.section`
  .label {
    font-size: 24px;
    font-weight: 400;
    margin: 40px 0;
  }
  .cards {
    display: flex;
    gap: 16px;
  }
`;
